using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class NumberButtonController : MonoBehaviour {
    // ボタンに設定するテキストと数値
    public string Text = "1";
    public int Number = 1;
    // ボタンのサイズを設定
    public int width = 70;
    public int high = 70;

	// Use this for initialization
	void Start () {
        // ボタンのサイズを設定する
        //this.GetComponent<RectTransform>().sizeDelta = new Vector2(this.width, this.high);
        //this.GetComponent<Button>();
        transform.GetComponent<OcclusionArea>().size = new Vector2(width, high);

    }
	
	// Update is called once per frame
	void Update () {
		
	}
}
